import * as ReactDOMClient from "react-dom/client";
import App from "./App";

// Use for as a placeholder for now
import "./styles/index.css";

ReactDOMClient.createRoot(document.getElementById("root")!).render(<App/>);
